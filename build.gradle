subprojects {
  apply plugin: 'java'
  version = '1.0'
}

project(':stalker') {
  dependencies {
    implementation project(':library')
  }
}

project(':harm') {
  dependencies {
    implementation project(':library')
  }
}